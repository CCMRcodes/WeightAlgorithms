% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Buta2018.R
\name{buta}
\alias{buta}
\title{Buta et al. 2018 Weight Cleaning Algorithm}
\usage{
buta(df, id, measures, tmeasures, outliers = c(11, 70))
}
\arguments{
\item{df}{object of class \code{data.frame}, containing \code{id} and
\code{measures}}

\item{id}{string corresponding to the name of the column of patient
identifiers in \code{df}}

\item{measures}{string corresponding to the name of the column of
measurements in \code{df}}

\item{tmeasures}{string corresponding to the name of the column of
measurement collection dates or times in \code{df}. If \code{tmeasures} is
a date object, there may be more than one weight on the same day, if it
precise datetime object, there may not be more than one weight on the same
day.}

\item{outliers}{numeric vector corresponding to the upper and lower bound of
\code{measures} for each time entry. Default is \code{c(11, 70)} for BMI
measurements according to Buta et al. 2018.}
}
\value{
returns a processed data frame with full removal of \code{id}
  groups and weight measurements, if not meeting criteria for inclusion.
}
\description{
\code{buta} returns a processed data frame with full removal of \code{id}
groups, if not meeting criteria for inclusion. It is dependent on patient
body mass index (BMI).
}
\examples{
library(dplyr)
data(cdw32)

cdw32 <- cdw32 \%>\%
  mutate(BMI = 703 * (Weight / (Height ^ 2)))

buta_df <- buta(
   df = cdw32,
   id = "id",
   measures = "BMI",
   tmeasures = "WeightDate"
  )

# dplyr::glimpse(buta_df)
}
